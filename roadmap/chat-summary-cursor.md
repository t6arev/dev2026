# Cursor Chat Summary — раздел «Услуги» (2026-07-02)

## Что сделали в этом чате

### Главная страница — блок «Услуги» / «Экспертиза»

- Убран sticky-блок `#services` с WebGL/Canvas-анимациями (пользователь отказался от motion на карточках услуг).
- После FAQ добавлен SEO-блок `services-hub-footer` (`#servicesHub`): 5 направлений, аккордеон из `services-data.js`.
- Порядок в nav: «Услуги» последним пунктом (после FAQ).
- Убран старый `seo-keywords-footer` (простой список ключей).
- Стили: glass/glow без жёстких рамок; убраны фоновые «рамки» у блока.
- Убран анимированный ротатор «Ключевые направления» — текст проваливался, был слишком мелкий.

**Файлы:** `index.html`, `css/services.css`, `js/services-render.js`, `js/services-data.js`

### Единый шаблон страницы услуги

Собран config-driven шаблон «как кейс, но под SEO»:

| Файл | Роль |
|------|------|
| `services/_template.html` | HTML-основа для копирования |
| `services/telegram-bots/index.html` | Первая эталонная страница |
| `js/service-page.js` | Рендер блоков, JSON-LD, форма |
| `js/services-data.js` | Контент + SEO по услуге |
| `css/services.css` | Hero, карточки, timeline, tech, FAQ, форма |
| `js/motion-scenes.js` | Canvas-сцена только в Hero (telegram) |

**Структура страницы (9 блоков):**

1. Hero (текст + Canvas/WebGL visual)
2. SEO-вводный текст (описание направления)
3. «Какие Telegram-боты мы разрабатываем» — glass-карточки
4. «Какие решения мы разрабатываем» — вертикальный список + badge кейса
5. Процесс — timeline с заполнением линии при скролле
6. Стек технологий — glass chips
7. «Почему заказать…» — карточки
8. Реализованные проекты — карточки из `caseData`
9. FAQ + простая форма (Имя / Telegram / Описание → TG)

**JSON-LD:** `BreadcrumbList`, `Service`, `FAQPage` (в `service-page.js`).

### Контент и SEO — `telegram-bots`

Переписан под кластер ключей из `вебмастер.md` (не «красивый общий текст», а посадочная):

- Высокий приоритет: разработка бота, чат-боты, telegram-боты под ключ, чат бот заказать…
- Средний: Telegram Bot API, Mini Apps, AI бот Telegram, стоимость…
- FAQ закрывает хвосты семантики (стоимость, под ключ, Mini App vs чат-бот, CRM, сроки).

**Статус услуги в конфиге:** `status: 'live'` (контент черновой, дизайн ещё на ревью).

### Исправления после ревью пользователя

| Проблема | Причина | Fix |
|----------|---------|-----|
| Шапка «поехала» | Breadcrumbs как `nav` + класс `section` | `div.service-breadcrumbs`, скрыты визуально (SEO в JSON-LD) |
| Кракозябры в nav | Битая кодировка в `telegram-bots/index.html` | Пересобран файл в UTF-8 |
| FAQ не виден | `.faq-item { opacity: 0 }` от motion без `is-in` | Override в `css/services.css` для `.service-page` |
| Форма «поехала» | Конфликт с глобальным `final-cta` | Scoped-стили `.motion.service-page .final-cta` |
| Смесь старого/нового | Кэш браузера | `?v=service-page-4` на CSS/JS |
| Карточки «дешёвые» | 5 колонок, мало воздуха | 3 колонки, glass + scan-line hover, крупнее типографика |

### Что НЕ сделано / не утверждено

- Дизайн страницы услуги **не финален** — идём по кускам: сначала стиль, потом текст.
- Остальные 4 услуги — shells без наполнения.
- Деплой, sitemap, GSC — не трогали.
- Объём текста ~1800–2500 слов на страницу — пока черновик, нужно добить.
- WebGL Hero для telegram — базовая canvas-сцена, не финальный Apple/Linear preset.

---

## Локально

```powershell
cd "C:\Users\t6are\OneDrive\Рабочий стол\моя визитка"
python -m http.server 8099
```

| URL | Что смотреть |
|-----|--------------|
| http://localhost:8099/ | Главная, блок `#servicesHub` внизу |
| http://localhost:8099/services/ | Hub услуг |
| http://localhost:8099/services/telegram-bots/ | **Эталон шаблона** |

Жёсткое обновление при странном виде: `Ctrl+Shift+R`.

---

## План следующих чатов (зафиксировано заказчиком)

1. **Единый формат страниц** — довести дизайн/стиль эталона (`telegram-bots`), зафиксировать шаблон.
2. **Корректировка текста** — SEO-кластеры из `вебмастер.md`, объём, естественные ключи.
3. **Блок услуг на главной** — в целом ок, возможно доработать детали.
4. **~10 SEO-страниц** — размножить шаблон (5 услуг + подразделы/кластеры).
5. **Финальный проход** — SEO, перелинковка, schema → деплой.
6. **Индексация** — новая sitemap, Яндекс.Вебмастер, Google Search Console, переобход.

---

## Ключевые файлы для следующего чата

```
services/_template.html          ← копировать под новую страницу
services/telegram-bots/index.html ← эталон
js/services-data.js              ← контент всех услуг
js/service-page.js               ← рендер + schema
css/services.css                 ← стили service page + hub footer
roadmap/services-branch-plan.md  ← трек ветки
roadmap/workflow/07-services-section.md
вебмастер.md                     ← семантика по кластерам
```

---

## Git

Изменения в рабочей директории, **коммит не делали** (по правилу — только по запросу).  
Ветка по git status на старте сессии: незакоммиченные файлы в `services/`, `js/services-*`, `css/services.css`, `index.html`, roadmap.

---

# Архив предыдущих сессий

## Обновление (2026-06-28)

### Motion + портфолио
- Motion-слой на всех 6 кейсах и главной
- FAQ анимация оптимизирована (`initFaqMotion`, один IO)
- Исправлены невидимые секции портфолио (`.motion .section { opacity: 1 }`, ранний `visible` в `main.js`)
- **Коллаж превью на главной:** несколько итераций; финал — без «рамки» (убраны фикс. высота + фон контейнера), картинка `height: auto`, на десктопе `width: 88%`, центр. Коммит `74a0edb`, задеплоено на VPS

### Google Search Console
- Meta-тег verification в `index.html` — коммит `3532ad2`
- Пользователь отправил sitemap: `https://dev2026.ru/sitemap.xml` (URL файла, не XML-текст)
- Добавлены 7 страниц на индексацию
- Sitemap на проде: HTTP 200

### Яндекс
- Sitemap OK (7 ссылок)
- Переобход после CSS-only правки коллажа **не обязателен**; опционально только главная

### Обсуждение SEO (записано в roadmap)
- Цель 100 чел/день ≈ 3000/мес — горизонт 12+ мес или реклама
- Пакет «800 запросов» = семантическое ядро, нужны страницы под фразы
- Траст E-E-A-T: посадочные, блог, Habr/VC, отзывы
- Микроразметка: на главной нет FAQPage; нет ProfessionalService на посадочных (ещё не созданы)

### Git
- `5139f1d` — invisible portfolio fix
- `3532ad2` — Google verification
- `74a0edb` — portfolio collage CSS

## Обновление (2026-06-25, утро)
- Доведена мобильная версия главной страницы:
  - исправлена мобильная шапка (`nav`) с корректной full-width геометрией;
  - зафиксирован порядок `hero` на телефоне: сначала заголовок/текст, потом фото;
  - уменьшено hero-фото на мобильных для более чистой композиции;
  - поджаты отступы и размеры контролов в модальной форме/квизе.
- Ускорена начальная загрузка первого экрана:
  - добавлен preload для `assets/img/ЭТО Я.jpg`;
  - hero-изображению заданы `width`/`height` для стабильной отрисовки;
  - облегчена мобильная фоновая анимация (`.blob`: меньше blur/opacity, медленнее цикл).
- Техническая проверка:
  - по измененным `index.html` и `css/main.css` — без linter-ошибок.
- Статус:
  - правки подготовлены к деплою; следующий шаг — выкладка и визуальная проверка на проде.

## Обновление (2026-06-25, день)
- Выполнен деплой на `main` (GitHub Pages).
- Создан и интегрирован новый кейс:
  - URL: `https://dev2026.ru/cases/telegram-bot-dlya-treydinga/`
  - файл: `cases/telegram-bot-dlya-treydinga/index.html`
  - изображение: `assets/img/кейс биржа.png`
  - кейс добавлен в `js/cases-data.js` и включен в порядок карточек на главной.
- Обновлен `sitemap.xml`:
  - добавлен URL нового кейса `telegram-bot-dlya-treydinga`.
- Что осталось по индексации:
  - подключить Google Search Console;
  - отправить `https://dev2026.ru/sitemap.xml`;
  - отправить через URL Inspection главную и приоритетные кейсы на индексирование.

## Что сделано в этом чате (2026-06-24)
- Доведены UI-правки на главной:
  - усилена шапка/hero визуально;
  - обновлен блок цен до 6 карточек;
  - увеличен floating widget и кнопка-пульс.
- Формы приведены к рабочему сценарию при недоступном API:
  - в `js/leads.js` добавлен fallback в Telegram `@t6arev`;
  - отправка заявки теперь не "падает молча", а уводит в TG с подготовленным текстом.
- Проект полностью закоммичен и запушен:
  - рабочая ветка `cursor-chat-summary` создана и запушена;
  - затем выполнен merge в `main`;
  - доп. коммит с Yandex verification file;
  - доп. SEO-коммит по ключам из `вебмастер.md`.
- Подтвержден Яндекс.Вебмастер:
  - файл `yandex_0cb5aee145484fa8.html` добавлен в корень и доступен на домене;
  - права подтверждены;
  - отправлены в переобход главная + 5 страниц кейсов.

## Что сделано по SEO-контенту
- На основе `вебмастер.md` отобраны коммерческие кластеры без мусорных "обучение/курсы/бренды конкурентов".
- Обновлены `title/description/keywords`, H1/lead, FAQ на:
  - `index.html`
  - `cases/ai-bot-telegram/index.html`
  - `cases/vpn-bot-telegram/index.html`
  - `cases/ai-assistant-business/index.html`
  - `cases/razrabotka-ii-agentov-ai-platforma/index.html`
  - `cases/ii-v-prodazhi-b2b-rassylki/index.html`

## Деплой и инфраструктура (факт)
- Публичный домен `dev2026.ru` сейчас обслуживается через GitHub Pages.
- Для фронта ручной деплой на VPS не нужен: изменения из `main` приезжают автоматически.
- Попытка поднять backend на VPS `62.113.36.165` выполнена, PM2-процесс запущен, но выдавал `502` по Telegram доставке:
  - Node-сервер `dev2026-api` онлайн;
  - зависимость `dotenv` установлена;
  - проблема осталась на шаге отправки в Telegram (диагностика API-ответа Telegram не завершена до конца).

## Текущий статус индексации
- В Яндекс.Вебмастере страницы отправлены в очередь переобхода.
- Ожидание: первичная реакция 1-3 дня, стабильная переиндексация до 1-2 недель.
- `robots.txt` и `sitemap.xml` добавлены и опубликованы, но `sitemap.xml` периодически отвечал нестабильно при внешней проверке (нужно мониторить).

## Что осталось сделать (приоритет)
1. Добить backend-доставку заявок на VPS или перевести API на другой хост (Railway/Render/VPS с рабочим Telegram outbound).
2. Завести Google Search Console:
   - подтвердить сайт;
   - отправить `https://dev2026.ru/sitemap.xml`;
   - отправить ключевые URL на индексацию.
3. Обновить цели Метрики (разделить по типам форм):
   - `lead_submit_quiz`
   - `lead_submit_callback`
   - `lead_submit_case`
   - оставить `tg_click`.
4. Добавить `assets/brand/logo.png` (сейчас опора в основном на SVG, часть ссылок указывает на PNG).

## Git / Коммиты (ключевые)
- `cc1f021` — крупный релиз (SEO-страницы, структура проекта, формы и UI).
- `cd8d62a` — merge `cursor-chat-summary` в `main`.
- `d629c76` — добавлен Yandex verification file.
- `576f36d` — SEO-усиление страниц по данным из `вебмастер.md`.
